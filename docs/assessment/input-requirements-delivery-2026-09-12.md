# Input requirements delivery — 12 September 2026

This release follows the Input Folder audit. Completion requires deployed acceptance, not just a passing unit test or a source file being registered.

## Release work

| Requirement | Implementation | Acceptance status |
| --- | --- | --- |
| WP10: complete client board packs | Measured table pagination shared by preview, PDF and editable PowerPoint. Every cell and exact value remains present; individual value links resolve governed evidence. Full provenance is retained in a PDF attachment and PowerPoint notes. No silent truncation. | Local full H1: 288 cells, 576 references; EN, AR and bilingual exports. Regression: 576-row tables in all languages. Production acceptance pending. |
| F9: readable evidence | Business labels replace claim UUIDs in chat. The fact link opens a readable page with the same current tenant, source, authority and lineage checks; technical identifiers remain collapsed. | Escaping and authorization regression passed. Production UI acceptance pending. |
| F3: language failure and deterministic fallback | Dedicated model-independent KPI-context endpoint. Disabled language configuration bypasses model calls. Slow responses request verified context at 12 seconds with a 3-second context deadline; completed language failures reauthorize figures before showing retry. Persona, tenant, BU, source and lineage rules apply; Board snapshots cannot be replaced with live context. Policy refusals can expose a source-provider permission-request action. | Unit and real PostgreSQL isolation checks passed. Production browser acceptance pending. The suggested 15-second threshold applies to displaying available context; normal Sol answers retain the existing bounded 60-second deadline, so working 15–25-second answers are not aborted. |
| WP7: separate seasonality input | Optional registered seasonal profile, per-cell positive factors and exact evidence references. Effective weight = history × seasonal factor × (1 + planning adjustment / 100). Target allocation retains the approved total exactly. Sources are rechecked during proposal, read, ratification and export; independent ratification replays the history-based calculation. The UI exposes separate inputs and source links; board exports retain allocation lineage and evidence. | Focused calculation, source-revocation, immutable-history and board-lineage tests passed. Full suite and extended three-person production UI lifecycle pending. |

## Remaining scope to reconcile

### Additional verification on 12 September

- The comparison/context chunk calculates actual-minus-plan only for one unambiguous pair with identical measure, entity, period, BU, scenario, currency and dimensions. Calculations retain both immutable input references and receive the calculated-insight tier; the provider cannot rewrite them. Complete recorded dimensions reach semantic selection, and source driver commentary is explicitly labelled as recorded commentary. Local regression: 2,615 passed, 163 service-dependent skips. Hosted acceptance remains pending.
- The fixed-corpus run on `a21c1d8` was stopped after repeated service failures. Twelve question results were retained, including a UI timeout; this is not a 50-question pass. Release and published source run stayed unchanged before/after the partial run. A read-only diagnostic found that a receivables question selects the broad `finance.transaction.amount` category: 11,276 records spanning AP, AR, GL and purchase orders, taking 33.56 seconds to authorize and forming 16 serial model batches. This identifies an overbroad retrieval design, not missing user data or exhausted database capacity. The gate remains open while retrieval is corrected.

- Q&A release `16c5b76` deployment 34696933629 is fully green: research 3/3, executive/Intent 10/10, Hermes 10/10. These confirm deployed behavior, not new publication of the candidate POC reports.
- Governed text-claim release `a21c1d8` passed CI 34697208485; deployment 34697779051 is in progress.
- The review-workspace chunk adds `/runs/review`, repairs `/app?lane=review` routing, lists recent runs and binds evidence, approval and operator resume to the selected run. It never substitutes the latest executive run after a lookup failure. Current source permissions are checked before checkpoint content is read; the display-only quotation projection preserves approval-bound source text. Local full regression: 2,598 passed, 163 service-dependent skips. The candidate browser read-through passed three steps on the existing approved run; deployment and a fresh approval/publication lifecycle remain pending. Failed candidate attempts are retained: the first exposed the unreachable legacy route; the second was a test route-handler signature error.

- `4e17a387cf076328ef8c9ccd16b3d3b3e2b1a7fa` deployment 34696126980 completed successfully. The supplemental board lifecycle passed all 20 steps against deployed files, without candidate overrides: 19 synthetic mutations, three identities, two denied self-approval checks, no external messages. Evidence is retained in `artifacts/input-requirements-2026-09-12/board-lifecycle-deployed-ui`. The historical candidate notes below are superseded for this lifecycle only.
- `16c5b76` passed CI 34695926637 and deployment 34696933629 is in progress. This is the source-reconciled Q&A calculation and paragraph-formatting release; final-answer auditor review and new immutable report publication remain open.
- The next correction includes authorized text claims in semantic fact selection. Source statements retain their exact text, claim status and immutable citation; missing/stale records remain excluded. Internal presentation-component names remain in lineage but are omitted from the visible answer. Tests cover mixed numeric/text retrieval, unavailable records, reported-claim status and HTML escaping. Deployment acceptance remains pending.
- A fixed 50-question browser collector now retains each answer, screenshot and independently resolved citation. Its one-question preflight succeeded on `4e17a38`; the question was answered with six traceable actual/plan facts. This is collection evidence, not an independently graded factual pass. The full corpus and G10 remain open.

- The following Q&A chunk replaces fixed account IDs and fixed source-row citations in the case-file EBITDA bridge with source-bound account classification and actual file ranges. Decimal GL/TB reconciliation, source hashes, duplicate/unmapped accounts, finite values, available roles and a positive revenue denominator gate the margin. Source-file changes and reconciliation failures now withhold the result with a reason. Candidate rendering against the supplied 185-file pack retains the ledger-scope EBITDA of SAR 215,741,310.56 and has 1/2/2 paragraphs across the three answers. Independent final-answer review and immutable publication remain outstanding; this is not a complete Task 3 acceptance claim.

- `693fd8a` deployment run 34694386636 is fully green: research 3/3, executive/Intent 10/10 (576 customer cells, three granular stories, desktop/mobile), Hermes 10/10. Supplemental deployed formula/scope 4/4 and missing-input draft creation passed. CI ran 2,735 tests with zero skips.
- An extended board lifecycle uncovered a real control-ownership race: Intent completion could enable another panel's pending controls, and the board composer discarded actions while busy. Controls now remain owned by their panel; board actions serialize and obsolete analysis work is ignored. Candidate hosted acceptance passed 20 steps with 19 synthetic UI mutations, three identities and two denied self-approval checks. It verifies a version-cited cell explanation, complete bilingual rows, unchanged old content marked stale after a newer actual import, and executive regeneration/download. Candidate results do not substitute for post-deployment acceptance.

- Release `f3d9ce4` is deployed: six selected-KPI context/failure browser cases, the 17-case mutation lifecycle (13 synthetic UI mutations, three personas, two denied-authority checks), ten Hermes cases and three research cases passed. Seasonal source export required the explicit export permission as designed. The historical deployment workflow remains red because its last test used CSP-blocked string evaluation; the corrected locator-based suite passed independently without changing CSP.
- Release `693fd8a` passed exact-SHA CI (Python services, Compose and Docker) and is entering deployment. It adds collapsed KPI technical provenance, removes unsourced legacy chart series/plan lines, and corrects optional source-contract serialization in missing-input draft requests. Candidate browser acceptance passed; deployed acceptance is recorded separately after rollout.
- The next case-file change gives the summary/top three a separate first page, preserves reviewed detector coverage on writer resume, strips only the outer model-facing quotation wrapper from human excerpts, embeds Arabic glyphs, and keeps finding headings with following content. The nine-page candidate was rendered and inspected. Existing published artifacts are immutable and have not been overwritten; this candidate is not a newly approved published report.

- WP10 full H1 export is deployed at `bd9dc74`: production EN/AR/bilingual UI exports each retained 288 cells and 576 references. Corrected research disclosure acceptance passed after the old selector matched two nested summaries. The historical deploy run itself remains red; its supplemental acceptance is recorded separately.
- Four KPI formula/scope walkthroughs passed using candidate JavaScript against production data. A remaining F9 defect in the KPI calculation disclosure was corrected: identifiers and source hashes now have a separate collapsed Technical details control. This candidate result is not yet deployed acceptance.
- The published POC run `fcf139fe-2acc-43ac-bb59-1cfd9cdd3bc5` contains all four requested files: case-file PDF, working-capital memo, Q&A transcript and audit log. Its eight locked findings total SAR 794,108; the audit log records challenges on four distinct findings. Files were copied read-only for inspection.
- Task 2 is **not closed**: the delivered memo explicitly reports settlement-day proxies, not balance-based DSO/DPO. The existing calculation averages the latest 13 invoice weeks and compares weeks within that same window; it does not implement the requested preceding 13-week baseline. A proper historical-balance/data-window implementation and numerical regression remain required.
- Task 3 contains the requested three topics and detailed citations, but the two-paragraph answer constraint and auditor review of each final answer still require verification/correction.
- Acceptance checks now reject absent auditor challenges, unrelated finding IDs, less than half challenged when more than eight findings exist, directories and empty files presented as deliverables. These checks establish minimum evidence presence, not the substantive validity of every artifact.
- Existing v2 document artifacts were located under `deliverables/kyvern-presentations`: all four technical DOCX files, three HTML decks and five SVG diagrams. Text inspection confirms Intent substrate, Business Intent Discovery, cadence monitoring, persona/write-back and local-model roadmap content. Several current-state descriptions still say read-only Vault; updating those descriptions and final artifact review remain separate from application test results.

- F6/F7/F13: complete formula/provider/request, answer-label and chart-scope walkthrough.
- WP7: final production seasonality acceptance, plan-version navigation and sector-pack removal proof.
- WP10: current-data bilingual exports, stale/regenerate lifecycle and persona/issue permissions in production.
- WP11: complete the specified configuration-console specification; this work order labels it SPEC, not a mandatory full console BUILD.
- v2 DOC/SPEC/ROADMAP: trace the specified briefs, decks, diagrams, Discovery Pack and roadmap commitments to their actual artifacts.
- POC task and agent contracts: verify case-file delivery, thirteen-week drift analysis, named drill-down questions and auditor challenge accounting.
- Question-bank coverage and demo-media deliverables: establish the expected acceptance scope and retain evidence for each delivered artifact; do not equate ten chat examples with the complete bank.

Actual email and external-system integrations remain deferred by the user's later instruction. Sol with medium reasoning is the authorized prototype model exception. Current POC monetary values remain SAR and NUPCO remains named, following the later dataset-specific direction.

## Release policy

Target: **https://strategyos.live**. Each chunk requires the full CI service suite, exact-release deployment and browser acceptance. Preserve the dedicated provider account and database runtime-role configuration. Do not replace concurrent development work or queue repeated deployments of failing revisions.
